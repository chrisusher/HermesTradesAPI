info:
  name: Get Portfolio
  type: http
  seq: 3

http:
  method: GET
  url: "{{HOST}}:{{PORT}}/api/v1/{{USER_ID}}/portfolios/{{PORTFOLIO_ID}}"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
