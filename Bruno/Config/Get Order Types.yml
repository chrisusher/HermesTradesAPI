info:
  name: Get Order Types
  type: http
  seq: 1

http:
  method: GET
  url: "{{HOST}}:{{PORT}}/api/v1/config/ordertypes"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
