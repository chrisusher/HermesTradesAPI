info:
  name: Get Portfolios
  type: http
  seq: 2

http:
  method: GET
  url: "{{HOST}}:{{PORT}}/api/v1/{{USER_ID}}/portfolios"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
