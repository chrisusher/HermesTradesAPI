info:
  name: Get Portfolio Stocks
  type: http
  seq: 4

http:
  method: GET
  url: "{{HOST}}:{{PORT}}/api/v1/{{USER_ID}}/portfolios/{{PORTFOLIO_ID}}/stocks"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
